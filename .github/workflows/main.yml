name: Test this action

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Test Slack action
      uses: frankkie/ga-pr-slack-notify@master
      with:
        webhook: ${{ secrets.slack_webhook }}
